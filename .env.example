# URL del servidor PHP para desarrollo local
# Vite redirigirá las llamadas /conta/api/* a este servidor
# Ejemplo: https://jramirez.eu (para usar el servidor de producción)
# Ejemplo: http://localhost:8080 (para usar PHP local con php -S)
VITE_API_TARGET=https://jramirez.eu/conta
